server:
  error:
    include-message: always
    include-binding-errors: always
  port:
    5000

spring:
  datasource:
    password: password
    url: jdbc:mysql://logistics.cwdngy7tu1w9.us-east-1.rds.amazonaws.com:3306/logistics
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
    show-sql: true